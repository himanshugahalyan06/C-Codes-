#include <bits/stdc++.h>
using namespace std;
int main()                                 //Two SUM Using Single Loop 
{
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }

    int target;
    cin >> target;
    if(target==target){
        
    }
    return 0;
}
